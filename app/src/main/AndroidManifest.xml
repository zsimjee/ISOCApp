<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isoc" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BanquetTicketsActivity" />
        <activity android:name=".EidPrayerInfoActivity" />
        <activity android:name=".GeneralDonationActivity" />
        <activity android:name=".JoinACommitteeActivity" />
        <activity android:name=".PowerOf1000Activity" />
        <activity android:name=".SadaqatulFitrActivity" />
        <activity android:name=".SponsorAnIftarActivity" />
        <activity android:name=".TodayAtISOCActivity" />
        <activity android:name=".ZakatActivity" />
        <activity android:name=".PaymentInfoActivity" />
        <activity android:name=".PersonConfirmationActivity" />
    </application>

</manifest>
